import { colors } from './../../../styles/colors'
const pins = {
  startPins: 3,
  pinSize: 2,
  pinGap: 20
}

const ball = {
  ballSize: 6
}

const engine = {
  engineGravity: 1.1
}

const world = {
  width: 390,
  height: 390
}

export const config = {
  pins,
  ball,
  engine,
  world,
  colors
}

import { colors } from './../../../styles/colors'
const pins = {
  startPins: 3,
  pinSize: 2,
  pinGap: 20
}

const ball = {
  ballSize: 5
}

const engine = {
  engineGravity: 1.1
}

const world = {
  width: 500,
  height: 500
}

export const config = {
  pins,
  ball,
  engine,
  world,
  colors
}

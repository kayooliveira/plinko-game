import { colors } from 'styles/colors'
const pins = {
  startPins: 3,
  pinSize: 3,
  pinGap: 20
}

const ball = {
  ballSize: 5
}

const engine = {
  engineGravity: 1
}

const world = {
  width: 390,
  height: 390
}

export const config = {
  pins,
  ball,
  engine,
  world,
  colors
}
